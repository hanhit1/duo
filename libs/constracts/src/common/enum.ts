export enum TheoryType {
  GRAMMAR = 'grammar',
  FLASH_CARD = 'flashcard',
  PHRASE = 'phrase',
}

export enum QuestionType {
  MATCHING = 'matching',
  ORDERING = 'ordering',
  GAP = 'gap',
  MULTIPLE_CHOICE = 'multiple_choice',
}
